@model List<Scheduling.Models.Department>
@{
    ViewBag.Title = "List Departments";
    Layout = "~/Views/Shared/_ResponsiveLayout.cshtml";
}


@{ if (Model.Count==0)
   {
       <p>There are no results</p>
    }
}

<div class='container'>
    <div class='sixteen columns'>
        @{
            if (Model.Count > 0)
            {
                  
                 @Html.Partial("_TableHeaderPartial", ViewData["header"] = "Departments")
                
            <table class='condensed zebra-striped' id='results'>
                <thead>
                    <tr>
                        <th>
                           Department
                        </th>
                                               
                    </tr>
                </thead>
                <tbody>
                    @{  foreach (Scheduling.Models.Department d in Model)
                        { 
           
                        <tr>
                            <td>@d.Description
                            </td>
                            
                           
                        </tr>
      
         
                        }
     
                    }
                </tbody>
            </table> 
 
        

            }


        }
    </div>

        <div id="pageNavPosition"></div>
  
        <script type="text/javascript"><!--
            var pager = new Pager('results', 10);
            pager.init();
            pager.showPageNav('pager', 'pageNavPosition');
            pager.showPage(1);
       </script>

</div>
